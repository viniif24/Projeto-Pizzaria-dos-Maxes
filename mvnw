#!/bin/sh
# ----------------------------------------------------------------------------
# Maven Wrapper
# ----------------------------------------------------------------------------
# Lightweight shell script to delegate to the maven wrapper jar
DIRNAME=`dirname "$0"`
BASEDIR=`cd "$DIRNAME" && pwd`
java -jar "$BASEDIR/.mvn/wrapper/maven-wrapper.jar" "$@"
